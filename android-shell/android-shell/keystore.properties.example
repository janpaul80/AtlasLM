# Patch 016B - keystore.properties TEMPLATE
# Copy to android/keystore.properties on the build machine and fill in.
# NEVER commit the real file or the .jks keystore. Add both to .gitignore.

storeFile=/absolute/path/to/atlaslm-release.jks
storePassword=CHANGE_ME
keyAlias=atlaslm
keyPassword=CHANGE_ME
